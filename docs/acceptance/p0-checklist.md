# P0 联调验收清单（管理端小程序）

## 1. 环境与账号

- [ ] Django 后端已启动
- [ ] 小程序 `baseUrl` 指向可访问后端
- [ ] 准备管理员账号（可登录 `/api/users/admin/login/`）

## 2. 登录与会话

- [ ] 从 `pages/mine/index` 进入“管理端入口”
- [ ] 输入正确账号密码可登录成功
- [ ] 登录后跳转 `pages-admin/dashboard/index`
- [ ] 清理 `admin_token` 后访问管理页会被引导回登录页

## 3. 控制台

- [ ] `GET /api/users/admin/dashboard/` 返回成功
- [ ] 页面正确显示：今日预约、待审核、用户总数、套餐总数
- [ ] 控制台可进入：套餐/预约/报告/用户/资料

## 4. 套餐管理（列表 + 基础 CRUD）

- [ ] `GET /api/packages/` 返回分页数据
- [ ] 套餐名称、价格、销量、状态显示正确
- [ ] 可新增套餐（至少字段：`name`、`price`）
- [ ] 可编辑套餐名称
- [ ] 可删除套餐
- [ ] 列表支持上拉加载更多
- [ ] 空数据时显示“暂无套餐数据”

## 5. 预约管理（列表 + 审核）

- [ ] `GET /api/appointments/` 可拉取分页列表
- [ ] 状态筛选可生效（待审核/已确认/已完成/已取消/已拒绝）
- [ ] 待审核订单可执行“确认”并成功
- [ ] 待审核订单可执行“拒绝”，并传递 `reject_reason`
- [ ] 列表支持上拉加载更多

## 6. 路由注册检查

- [ ] `app.json` 已注册 `pages-admin/reports/list`
- [ ] `app.json` 已注册 `pages-admin/users/list`
- [ ] `app.json` 已注册 `pages-admin/profile/index`
- [ ] 新增页面打开无“页面不存在”错误

## 7. 异常处理

- [ ] token 过期时弹出提示并回到登录页
- [ ] 后端返回业务错误时展示 `message`
- [ ] 网络断开时显示“网络错误”提示

## 8. 本轮收尾记录（2026-04-01）

- 范围：P1 首批入口打通（报告/用户/资料）+ 路由注册补齐
- 结果：页面文件与导航已接入，待测试同学按清单逐项验收
- 遗留：文章管理等 P1 后续批次

## 8.1 本轮收尾记录（2026-04-08）

- 范围：用户端主链路收口 + 管理端轮播管理接入
- 结果：`pages-admin/banners` 已支持列表/新增/编辑/删除/启停；后端 `SmokeTest` 扩展到 14 条并通过
- 遗留：轮播管理清单中的人工验证项（未登录跳转、空数据页、接口失败提示场景）待实测确认

- [ ] 预约创建手机号格式错误时前端拦截并提示
- [ ] 预约创建身份证格式错误时前端拦截并提示
- [ ] 预约日期早于今天时前端拦截并提示
- [ ] 在线咨询待回复记录可撤回，已回复记录不可撤回
- [ ] 报告列表空状态显示“暂无已出具体检报告，可先在预约页查看进度”
- [ ] 预约/咨询/报告页提示文案与常量模块保持一致（避免散落硬编码）

